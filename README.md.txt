# Solubility Model with Fingerprints

This repository contains a script that trains a model using Morgan fingerprints for the Solubility dataset.

## Requirements
Install the dependencies by creating a Conda environment with the provided `environment.yml` file.

```bash
conda env create -f environment.yml
